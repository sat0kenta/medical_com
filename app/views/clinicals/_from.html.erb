<% clinicals.each do |clinical| %>
  <%#= link_to clinicals_path(clinical.id) do %>
    <div class="col-3">
      <div class="card m-1">
        <div class="card-body">
        <td>
          <% if user_signed_in? %>
            <%= link_to clinical.title,clinical_path(clinical.id) %>
          <% else %>
            <%= clinical.title %>
          <% end %>
        </td>
        <h1></h1>
        </div>
      </div>
    </div>
  <%# end %>
<% end %>